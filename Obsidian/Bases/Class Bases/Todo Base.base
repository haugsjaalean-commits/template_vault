filters:
  and:
    - file.isA("Todo")
    - '!file.inFolder("Obsidian/Templates")'
formulas:
  word count: file.WordCount()
  link distance: file.linkDistance()
  value: (payoff/difficulty).round(2)
  is a contains self: file.isA() == self
  is_is_a: this.file == note["is a"]
  is_type_of: this.file == note["TYPE OF"]
  connection type: if(formula["is_is_a"], "is a", if(formula["is_type_of"], "type of", "misc"))
  Untitled: ""
  file: file
  is is a: this.file == note["is a"]
  is type of: this.file == note["TYPE OF"]
properties:
  formula.word count:
    displayName: word count
  formula.link distance:
    displayName: link distance
views:
  - type: table
    name: Todo Only
    filters:
      or:
        - note["is a"].contains(link("Todo"))
        - note["is a"].contains(link("Time Sensitive Todo"))
    groupBy:
      property: done
      direction: ASC
    order:
      - done
      - file.name
      - urgence
      - formula.value
      - payoff
      - difficulty
      - date
      - project
      - is a
  - type: table
    name: Table
    groupBy:
      property: is a
      direction: DESC
    order:
      - done
      - file.name
      - urgency
      - payoff
    sort:
      - property: done
        direction: ASC
